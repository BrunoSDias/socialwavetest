<div class="page-header">
  <%= link_to comprador_path(@pedido.comprador.id), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    <%= t 'buttons.back' %>
  <% end %>
  <h1>Pedido n°<%= @pedido.id %></h1>
</div>

<%= render 'form' %>
