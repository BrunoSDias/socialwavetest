<div class="page-header">
  <%= link_to comprador_path(@comprador), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    <%= t 'buttons.back' %>
  <% end %>
  <%= link_to @comprador, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    <%= t 'buttons.show' %>
  <% end %>
  <h1>Alterar Conta</h1>
</div>

<%= render 'form' %>
