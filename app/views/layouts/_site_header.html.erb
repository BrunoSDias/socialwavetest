<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="navbar-header">
    <%= link_to root_path do %>
      <span class="navbar-brand">SocialWave</span>
    <% end %>
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
      <span class="sr-only">Navegação</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
  </div>
  <div id="navbar" class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-right top-nav">
      
      <!-- condicional verdadeiro -->
      <% if cookies[:comprador_login].present? %>
      <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><i class="glyphicon glyphicon-user"></i> <%= JSON.parse(cookies[:comprador_login])["nome"] %> <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to comprador_path(JSON.parse(cookies[:comprador_login])["id"]) do %>
                <i class="glyphicon glyphicon-file"></i> Minha conta
              <% end %>
              <%= link_to deslogar_path do %>
                <i class="glyphicon glyphicon-off"></i> Sair
              <% end %>
            </li>
          </ul>
      </li>

      <!-- condicional falso -->
      <% else %>
      <li>
        <%= link_to login_index_path do %>
          <i class="glyphicon glyphicon-user"></i> Entrar
        <% end %>
      </li>
      </li>
        <li>
          <%= link_to new_comprador_path do %>
            <i class= "glyphicon glyphicon-plus"></i> Cadastrar-se
          <% end %>
        </li>
      <% end %>

    </ul>
  </div>
</nav>
